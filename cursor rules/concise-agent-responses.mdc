---
description: 默认简短回复，节省对话上下文；详述仅在用户明确要求时展开
alwaysApply: true
---

# 助手回复：默认精简（省上下文）

除非用户**明确要求**长说明、教程式讲解或全文 diff，否则助手应默认采用**短回复**，降低后续轮次占用的上下文容量。

## 应当怎么做

- **优先给出**：变更列表（做了什么）、**涉及文件路径**（`Assets/...`）、必要时 **三句话以内** 的结论（为何这么改）。
- **需要指向已有代码时**：仍用规范 **代码引用块**（`startLine:endLine:filepath`），但只引用必要片段，避免大段无关上下文。
- **避免**：在同一回复里重复粘贴整文件、重复复述用户已可见的长 diff、长篇前言后语；用户要自己看 git / diff 时不必再抄一遍。

## 反例 / 正例

- **反例**：用十几段话复述本次修改的每一步，并把多个完整 `.cs` 文件再贴一遍。
- **正例**：「改了 `CardController`：加交换动画与放置锁；`ConfigureGameplayCards` 增加 `swapMoveDuration`。详见上述路径。」

若用户写「详细说明」「展开讲讲」「把代码贴全」，再切换到完整说明模式。
