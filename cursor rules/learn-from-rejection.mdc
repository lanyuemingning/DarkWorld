---
description: 用户否定代码时，把隐含要求写入 rules 以防再犯
alwaysApply: true
---

# 从否定中沉淀规则

当用户**明确否定**某次实现（例如撤销 diff、说「不对」「不要这样」「别用 X」、或拒绝合并某写法）时：

1. **先对齐原因**：用一两句话复述用户反对的点（技术选型、分层、命名、性能、API 用法等），不确定则简短追问一次。
2. **写成可执行约束**：把「不要做什么」和「应当怎么做」落成一条**具体、可检查**的条目（避免空泛的「写好一点」）。
3. **写入项目规则**：把该条目追加到最合适的 `.cursor/rules/*.mdc`（与主题相关的文件）；若没有合适文件，可新建窄主题规则（单文件单主题，保持简短）。
4. **格式**：与其它规则一致，使用 YAML frontmatter；新增内容用列表项，必要时加最小反例/正例。
5. **不重复堆砌**：写入前扫一眼现有 rules，若已有同类条款则**合并或强化**一条，避免多条重复表述同一要求。

之后在本仓库中生成代码时，默认遵守已写入的 rules，避免再次触发同类否定。
